# Frontend — React + Vite

Patient browser for the migrated FHIR data. See the [root README](../README.md) for full setup instructions.

```bash
npm install
npm run dev
```

Expects the backend API at `http://localhost:8000/api` (override with `VITE_API_BASE`).
